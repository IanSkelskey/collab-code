# WebSocket server URL for Yjs document sync.
# In development, the local server defaults to ws://localhost:4444.
# For production, set this to your deployed server (use wss:// for TLS):
#   VITE_WS_URL=wss://collab-code-sync.your-domain.com
VITE_WS_URL=wss://collab-code-33z5.onrender.com
