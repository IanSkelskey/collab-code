# Render deployment blueprint for the Yjs sync server.
# See: https://docs.render.com/infrastructure-as-code
services:
  - type: web
    name: collab-code-sync
    runtime: docker
    plan: free
    rootDir: server
    envVars:
      - key: HOST
        value: 0.0.0.0
    healthCheckPath: /
